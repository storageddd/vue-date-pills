export enum EMode {
  month = 'month',
  year = 'year'
}

export enum EEvents {
  update = 'update:modelValue',
  change = 'change'
}

export enum ELabelFormat {
  month = 'MMMM',
  monthYear = 'MMMM YYYY',
  year = 'YYYY'
}
